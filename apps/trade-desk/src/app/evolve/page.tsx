import { EvolveDesk } from "@/components/evolve/EvolveDesk";

export const metadata = {
  title: "Evolve · Trade Desk",
  description:
    "Model evolution farm — utility ranks, claim levels, multi-lock finalize",
};

export default function EvolvePage() {
  return <EvolveDesk />;
}
