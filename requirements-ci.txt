-r requirements-runtime.txt

# Tests exercise parquet-backed causal folds in addition to the live runtime.
pyarrow==19.0.1
pytest==9.1.1

# Provides the `backtest` package used by tools/ and tests/.
vibe-trading-ai==0.1.11

