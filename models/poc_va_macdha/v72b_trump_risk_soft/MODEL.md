# v72b_trump_risk_soft

Soft-size Trump-era pipeline: **v39b_live_adapt** teacher + continuous risk scale with optional **crypto-corridor** (COIN/MSTR when present in `data_map`).

Distinct from `v72_trump_risk_hard` (different teacher, soft vs hard policy, crypto weight).
