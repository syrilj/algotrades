# v36_genome_train

Secondary-genome wrapper over **v23_devin_overlay**.

- `_base_engine.py` — original v23 signal engine (SIDE)  
- `signal_engine.py` — train-loop wrapper (scales / thresholds signals from `GENOME.json`)  
- `GENOME.json` — frozen knobs from `runs/evolve_brain/BEST_GENOME.json`  

Use for research / paper comparison. Desk default remains WINNER until full CLAIM promote.
