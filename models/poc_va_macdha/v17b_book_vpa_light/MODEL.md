# v17b_book_vpa_light

Light Coulling gates: `block_no_demand` + `require_commitment` + `allow_stopping_reclaim`.

**Result:** PASS bar. PF beats v15; DD better; Sharpe/return still below v15.

Portable finding for GEX: combine with `vol_z` — see `models/poc_va_gex/artifacts/BOOK_VPA_META.json`.
