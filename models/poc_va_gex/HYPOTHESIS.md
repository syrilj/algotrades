# Hypothesis — poc_va_gex

Dealer gamma regime and underlying volume shocks are **conditional amplifiers** of our existing stock primary signals.

- When net GEX is negative and underlying volume z ≥ 2, trend-aligned long candidates from `poc_va_macdha` should show **higher OOS win rate / expectancy**.
- When net GEX is positive and volume is quiet, the same candidates should be **downsized or skipped** (pin / mean-reversion tape).

We will prove volume-z first (full history available), then GEX once historical option OI is available. Stock primary SIDE stays unchanged.
